<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- 定义边框颜色和宽度 -->
    <stroke android:width="6dp" android:color="#4CAF50" />
    <!-- 如果需要，可以定义背景颜色 -->
    <solid android:color="@color/white1" />
    <!-- 可选：如果需要圆角 -->
    <corners android:radius="10dp" />
</shape>
